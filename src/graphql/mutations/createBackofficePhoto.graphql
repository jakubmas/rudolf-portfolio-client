mutation CreateBackofficePhoto($input: BackofficePhotoInput!) {
  createBackofficePhoto(input: $input) {
    errors {
      field
      message
    }
    backofficePhoto {
      id
      title
      photoUrl
      createdAt
      updatedAt
    }
  }
}
